# S30 live smoke (2026-09-09)

Deploy URL: https://x-livespaces.dionisio-lopez.workers.dev

Coordinator (Hazard) verified the write-risk items live. This slice did **not** rotate or delete `X_API_BEARER_TOKEN`, did **not** empty KV, and did **not** repeat the rate-limit burst.

## Coordinator (done — do not redo)

| Check | Result |
| --- | --- |
| Rate limit | 75 sequential GET `/api/spaces` in one window → 60×200 + 15×429. Body `kind: x-api-rate-limited`. Parallel bursts under-count (KV RMW races); sequential is the deterministic check. |
| Last-good on X failure | Invalid bearer: POST refresh → `refreshed: true`, `coverage: cached-after-failure`, prior cards served, KV `generatedAt` unchanged. |
| Missing bearer | Secret deleted: POST refresh HTTP 500; GET `/api/spaces` 200 last KV. Real bearer restored afterward. |
| Cooldown skip | Verified live (immediate second POST does not fan out). |
| Cold empty | Unit tests + pre-deploy; not re-run against live KV (would wipe the board). |

## This slice (read-only)

| Check | Result |
| --- | --- |
| GET `/` | 200 |
| GET `/api/spaces` | 200, `stale: false`, `coverage: official-search`, `liveCount: 87`, `generatedAt: 2026-09-09T08:36:44.611Z` |

No POST `/api/spaces/refresh` from this slice (board already fresh; cooldown skip already verified).
